CREATE TABLE dbo.Widget
(
    WidgetId INT NOT NULL PRIMARY KEY NONCLUSTERED,
    Amount INT NULL,
    Note NVARCHAR(50) NULL,
    INDEX IX_Widget_Amount NONCLUSTERED (Amount) INCLUDE (Note)
) WITH (MEMORY_OPTIMIZED = ON);
