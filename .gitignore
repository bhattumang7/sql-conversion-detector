bin/
obj/
artifacts/
TestResults/
*.user
.vs/
.idea/
.vscode/

# Cloned corpus repositories are never checked in; only the manifest is.
corpus/*
!corpus/manifest.json
!corpus/manifest.schema.json

# Sonar scanner working data
.sonarqube/
.scannerwork/
.sonar-scan.lock
project references.txt
scan-report.txt

# Local dev secrets (SA password override for docker-compose)
.env

# Remote database backups (never checked in)
backups/

# Disposable dynamic-SQL rebuild scaffolding (docs/dynamic-sql-rebuild-plan.md Phase 0/6)
docs/.rebuild-baseline/

# CLAUDE.md is tracked with a sanitized version; the local copy carries
# credentials and is protected via `git update-index --skip-worktree CLAUDE.md`

# Third-party proprietary binaries kept locally for reference/comparison only —
# never redistributable, must never be committed or pushed.
vendor/
# (this ignore entry is belt-and-braces for any untracked state)
local-test-database.md
